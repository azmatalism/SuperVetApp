import React from "react"
import {  View } from 'react-native'

function CharactersGlobal({ }) {
    return (
        <View style={[ { marginBottom: 6 }]}>
        </View>
    )
}

export default CharactersGlobal;